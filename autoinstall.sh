yum -y update && yum -y install nano && yum -y groupinstall  "Development Tools" && wget http://repo.sufanet.com/centos6/epel-release-6-8.noarch.rpm && rpm -ivh epel-release-6-8.noarch.rpm && yum  -y  install  dropbear && wget http://repo.sufanet.com/centos6/webmin-1.630-1.noarch.rpm && rpm -U webmin-1.630-1.noarch.rpm && chkconfig webmin on && /sbin/chkconfig dropbear on && wget http://repo.sufanet.com/centos6/dropbear && rm -f /etc/sysconfig/dropbear && cp dropbear /etc/sysconfig/dropbear && /etc/init.d/httpd stop &&  yum -y remove postfix && yum -y remove sendmail && /sbin/chkconfig dropbear on && wget http://repo.sufanet.com/centos6/sshd_config && cp --force sshd_config /etc/ssh/sshd_config && /etc/init.d/dropbear start && /etc/init.d/sshd restart && yum -y install squid && wget https://github.com/codem0v/test/blob/master/squid.conf && cp --force /etc/squid/squid.conf && chkconfig --add squid && chkconfig squid on && service squid restart && rm -rf autoinstall.sh  epel-release-6-8.noarch.rpm  webmin-1.630-1.noarch.rpm dropbear sshd_config
